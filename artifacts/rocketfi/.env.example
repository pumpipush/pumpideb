# ── Frontend Build Environment Variables ─────────────────────────────────────
# These are baked into the frontend bundle at build time (VITE_* prefix)
# Set these before running: pnpm run build

# Base URL path (use / for root domain, /app for subdirectory)
BASE_PATH=/

# Alchemy API key for Solana RPC (wallet transactions, price lookups)
VITE_ALCHEMY_API_KEY=

# ── Platform Fee ──────────────────────────────────────────────────────────────
# Your Solana wallet address — receives 1% on all trades + flat fee on token creation
# If not set, NO fees are collected (all transactions go fee-free)
VITE_PLATFORM_FEE_RECIPIENT=YourSolanaWalletAddressHere

# ── Optional ──────────────────────────────────────────────────────────────────

# Google OAuth client ID (must match GOOGLE_CLIENT_ID on the API server)
# VITE_GOOGLE_CLIENT_ID=

# Custom Solana RPC URL (overrides Alchemy if set)
# VITE_SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# ── GitHub Actions CI secrets (set in repo Settings → Secrets → Actions) ─────
# These are NOT frontend env vars — they are only used by the smoke test workflows.

# Alchemy API key for smoke tests (optional — falls back to PublicNode free RPC)
# GitHub secret name: ALCHEMY_API_KEY

# Active pump.fun bonding-curve token address for the buy-fee smoke test.
# Replace when the default token graduates off the bonding curve (test will warn when this happens).
# GitHub secret name: SMOKE_PUMP_TOKEN
# Default (built into test): TBZqyJGTGxLEtHgRSG8YUvmikUxCFsKCgSctqxqpump
