/** raydium-launchlab.ts — stub (adapter disabled, pending per-DEX planning) */
import { logger } from "../logger";

export async function startRaydiumLaunchLabAdapter(): Promise<void> {
  logger.info("raydium_launchlab: adapter disabled — pending planning");
}
