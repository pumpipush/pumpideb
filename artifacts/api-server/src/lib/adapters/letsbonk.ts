/** letsbonk.ts — stub (adapter disabled, pending per-DEX planning) */
import { logger } from "../logger";

export async function startLetsBonkAdapter(): Promise<void> {
  logger.info("letsbonk: adapter disabled — pending planning");
}
