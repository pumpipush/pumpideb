/** moonshot.ts — stub (adapter disabled, pending per-DEX planning) */
import { logger } from "../logger";

export async function startMoonshotAdapter(): Promise<void> {
  logger.info("moonshot: adapter disabled — pending planning");
}
