/** daos.ts — stub (adapter disabled, pending per-DEX planning) */
import { logger } from "../logger";

export async function startDaosFunAdapter(): Promise<void> {
  logger.info("daos_fun: adapter disabled — pending planning");
}
